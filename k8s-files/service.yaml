apiVersion: v1
kind: Service
metadata:
  name: bank-website-service
  namespace: bank-app
spec:
  type: NodePort
  selector:
    app: bank-website
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
